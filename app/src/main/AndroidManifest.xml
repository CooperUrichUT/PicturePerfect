<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:supportsRtl="true"
        android:theme="@style/Theme.PicturePerfect"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity">
            <!-- Add any additional attributes as needed -->
        </activity>
        <activity android:name=".ui.login.RegisterActivity">
            <!-- Add any additional attributes as needed -->
        </activity>
        <activity android:name=".ui.dashboard.NextActivity">
            <!-- Add any additional attributes as needed -->
        </activity>
        <activity android:name=".ui.profile.DashboardProfileActivity">
            <!-- Add any additional attributes as needed -->
        </activity>
        <activity android:name=".ui.profile.ProfileNextActivity">
            <!-- Add any additional attributes as needed -->
        </activity>
    </application>

</manifest>